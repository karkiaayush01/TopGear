{
  "Note": "This is a sample for appsettings.json. Craete a new appsettings.json file in the same path, copy everything here and replace the connection string with your proper db credentials. You can remove this Note key in the actual file.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "Postgres": "Host=localhost;Port=5432;Username=postgres;Password=****;Database=****"
  }
}